{
  "name": "frontend-demos",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "demos/*"
  ],
  "scripts": {
    "dev:khara": "npm run dev --workspace khara-collective",
    "build": "npm run build --workspace khara-collective && node scripts/build-pages.mjs",
    "build:khara": "npm run build --workspace khara-collective",
    "preview:khara": "npm run preview --workspace khara-collective"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
